/**
 * Deprecated booking modal notice:
 * The modal-based booking flow is intentionally kept in the codebase as a backup.
 * It is temporarily unused in the UI while the new section-based multi-step stepper
 * experience is active. Do not delete until the new flow is fully validated in production.
 */
export function DeprecatedBookingModalNotice() {
  return null;
}
